.theos
packages/
build/
.DS_Store
.vscode
tests/harness
tests/harness.d
tests/*.gcno
tests/*.gcda
tests/*.gcov
artifacts/
# Built framework staged by build.sh stage_deps(); theos links against it
# via _THEOS_INTERNAL_SEARCHPATHS. Never a source checkout.
vendor/HookKit.framework/
.detector-deps/
core
core.*
# Stray Swift module blobs a mis-cwd'd build can drop at the repo root.
/Swift-*.swiftmodule
__pycache__/
*.py[cod]
# Private harness checkout mount (sibling shadow-private repo). Never
# committed; never force-add. Maintainer-local notes live there too.
private/
