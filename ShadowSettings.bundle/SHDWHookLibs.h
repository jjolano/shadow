#import <Foundation/Foundation.h>

// Substrate/Substitute are obsolete swizzling backends — excluded from the
// picker (auto/ElleKit/fishhook only).
NSArray<NSDictionary *> *SHDWAvailableHookLibs(void);
