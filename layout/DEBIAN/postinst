#!/usr/bin/env sh
set -e

case "$1" in
    configure)
        chown -R mobile:mobile /Library/Shadow
        chmod -R 0777 /Library/Shadow
        /usr/local/bin/shdw -g || true
    ;;

    triggered)
        /usr/local/bin/shdw -g || true
    ;;
esac

exit 0
