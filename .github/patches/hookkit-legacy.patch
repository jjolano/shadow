diff --git a/Backends/HKInlineBackends.m b/Backends/HKInlineBackends.m
--- a/Backends/HKInlineBackends.m
+++ b/Backends/HKInlineBackends.m
@@ -12,11 +12,11 @@
 // plain C and safe to include, but the backend class below is arch-gated too
 // so armv7 builds never reference DobbyHook/DobbyCodePatch at link time.
-#if defined(__arm64__) || defined(__arm64e__)
+#if (defined(__arm64__) || defined(__arm64e__)) && !defined(HK_NO_DOBBY)
 #include "dobby/dobby.h"
 #endif
 
 #pragma mark - HKDobbyBackend
 
-#if defined(__arm64__) || defined(__arm64e__)
+#if (defined(__arm64__) || defined(__arm64e__)) && !defined(HK_NO_DOBBY)
 @implementation HKDobbyBackend
 - (int)lastErrno {
diff --git a/HKBackendRegistry.m b/HKBackendRegistry.m
--- a/HKBackendRegistry.m
+++ b/HKBackendRegistry.m
@@ -53,7 +53,8 @@ static BOOL swift_available(void) {
 
     if(!cached) {
         if(hk_swift_supported()) {
-            if(@available(iOS 12.2, *)) {
+            NSOperatingSystemVersion swiftFloor = { 12, 2, 0 };
+            if([[NSProcessInfo processInfo] isOperatingSystemAtLeastVersion:swiftFloor]) {
                 hk_swift_demangle = (hk_swift_demangle_fn)dlsym(RTLD_DEFAULT, "swift_demangle");
 
                 if(!hk_swift_demangle) {
@@ -82,7 +83,7 @@ static BOOL swift_available(void) {
 // Dobby is compiled in on arm64/arm64e only (the vendored static lib has no
 // armv7 slice); the table entry stays on every arch so the count is stable.
 static BOOL dobby_available(void) {
-#if defined(__arm64__) || defined(__arm64e__)
+#if (defined(__arm64__) || defined(__arm64e__)) && !defined(HK_NO_DOBBY)
     return YES;
 #else
     return NO;
diff --git a/Makefile b/Makefile
--- a/Makefile
+++ b/Makefile
@@ -56,7 +56,10 @@ include $(THEOS_MAKE_PATH)/framework.mk
 # Dobby: vendored static lib, arm64/arm64e slices only (no armv7), C++
 # implementation so it needs libc++ at link. Excluded from the legacy
 # armv7/armv7s build by the ARCHS filter.
-ifeq ($(filter arm64,$(ARCHS)),arm64)
+ifeq ($(HOOKKIT_LEGACY),1)
+HookKit_CFLAGS += -DHK_NO_DOBBY
+HookKit_LDFLAGS += -lc++ -nodefaultlibs -lSystem
+else ifeq ($(filter arm64,$(ARCHS)),arm64)
 HookKit_LDFLAGS += -Lvendor/dobby -ldobby -lc++
 endif
 
@@ -68,6 +73,7 @@ endif
 # global ARCHS: that lets the framework span all four slices in one pass while
 # gum stays 64-bit. Rootless packaging maps /usr/lib -> /var/jb/usr/lib
 # automatically.
+ifneq ($(HOOKKIT_LEGACY),1)
 LIBRARY_NAME = HKGum
 HKGum_FILES = vendor/gum/hkgum.c
 HKGum_ARCHS = arm64 arm64e
@@ -76,6 +82,7 @@ HKGum_ARCHS = arm64 arm64e
 HKGum_LDFLAGS = -Lvendor/gum -lfrida-gum -exported_symbols_list $(CURDIR)/scripts/export-HKGum.list
 HKGum_INSTALL_PATH = /usr/lib
 include $(THEOS_MAKE_PATH)/library.mk
+endif
 
 # Release export check: verifies every built binary exports exactly its
 # allowlist (scripts/export-*.list), per arch slice. Discovers the freshly
